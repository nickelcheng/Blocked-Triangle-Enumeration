#ifndef __IO_H__
#define __IO_H__

#include<vector>
#include "main.h"

using namespace std;

int inputEdge(const char *inFile, vector< Edge > &edge);
void printEdge(const vector< Edge > &edge);

#endif

